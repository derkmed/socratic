# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
*.egg-info/

# Isolated worktrees for parallel agents — local scratch, never committed.
/.claude/worktrees/
