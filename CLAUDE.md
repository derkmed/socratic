# socratic

## Agent skills

### Issue tracker

Issues live in GitHub Issues on `derkmed/socratic`, driven through the `gh` CLI.
See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each label string equal to its name. See
`docs/agents/triage-labels.md`.

### Domain docs

Single-context, with the glossary at `docs/CONTEXT.md` and decisions in
`docs/adr/`. See `docs/agents/domain.md`.
